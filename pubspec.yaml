name: project_assignment_e
description: MOSIQATI - Musical Instruments Store App for Jordan

publish_to: 'none'

version: 2.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  http: ^1.2.0
  intl: ^0.20.2
  flutter_rating_bar: ^4.0.1
  share_plus: ^9.0.0
  video_player: ^2.9.1
  device_preview: ^1.2.0
  shared_preferences: ^2.3.2
  firebase_core: ^3.4.0
  firebase_auth: ^5.2.0
  cloud_firestore: ^5.4.0
  firebase_storage: ^12.2.0
  image_picker: ^1.2.2
  url_launcher: ^6.3.2
  audioplayers: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/audio/
